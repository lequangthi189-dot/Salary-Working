One-line: the boot loader — bouncing ball + one plain label.

```jsx
<Loader label="Loading…" overlay />
```

Use it only while the session/profile is unknown. Inside a screen, use `Skeleton` shapes instead so the layout does not jump.
